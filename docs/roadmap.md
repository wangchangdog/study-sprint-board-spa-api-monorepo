# ロードマップ

この見本は MVP を完成形として固定するのではなく、比較教材として少しずつ拡張できるように設計しています。

## 近い拡張

1. タスク一覧の検索、フィルタ、ソート
2. ページネーション
3. ラベル管理画面
4. admin 向け操作 API の追加

## 契約まわりの拡張

1. OpenAPI から Web 用 API client を生成する
2. OpenAPI の差分チェックを CI に入れる
3. TypeSpec を記述元にし、OpenAPI を生成成果物にする

## 認証まわりの拡張

1. OAuth 連携
2. セッション更新と失効管理の強化
3. 監査ログの追加

## UI / 運用の拡張

1. `packages/ui` を本格利用して共通 UI を切り出す
2. Storybook などの UI 単体確認環境を追加する
3. 本番向けの監視、ログ、ヘルスチェック運用を整理する

## TODO として残していること

- TypeSpec はまだ導入していません
- admin 用 API の認可はまだ最小構成です
- 本番向けデプロイ設定ファイルは見本 repo に含めていません
- E2E は demo モード中心で、分離配備先に対する本番相当検証は未追加です
